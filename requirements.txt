# Optia Agentic Workshop - Python deps. Fastest setup (avoids PEP 668):
#   python3 -m venv .venv && source .venv/bin/activate   # Windows: .venv\Scripts\activate
#   pip install -r requirements.txt
# then select the .venv interpreter as your notebook kernel. See SETUP.md.

# No third-party packages required. The notebooks call the model over plain HTTPS using the
# Python standard library (urllib), so they run on locked-down machines with no pip access.
# You only need Python 3.10+.
